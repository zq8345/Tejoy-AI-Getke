# 开发日志

> 无人值守期间的执行记录。按时间倒序追加。总工用 `git log` + `git diff` + 本文件验收。
> 分支 `refactor/funnel-toolbars`，baseline `e9ae402`。

---

## 2026-07-15 · 🏁 验收报告（队列①～⑦ 全部做完，停下等总工）

分支 `refactor/funnel-toolbars`，baseline `e9ae402`，共 10 个 commit，`+693 / -293`（6 个文件）。
**未 deploy、未碰生产库、未碰 M3 护栏、未覆盖 followup/discovery 开关、未 `git add -f` 任何密钥。**

### 一、每批做了什么 + 实测结论

| # | 批次 | commit | 做了什么 | 实测结论 |
|---|---|---|---|---|
| ① | H3-v2 打分闸 | `f148492` | 按 Joe 的 A 方案删体量门槛，唯一判据＝在不在卖/装实体硬件 | ✅ **通过**，详见下方队列①条目。Speedcast 30→85、Telstra 仍 30（理由换成"竞品"）、纯内容站 20 未回潮 |
| ② | 批③C 九格工具栏 | `88acd4c` | 删全局工具栏（含 `current="all"` 的旧 handler），改成每格只出该格的出口动作；待审批加「缺邮箱／低于门槛」筛选 | ✅ 九格逐格点过，每格只出该格动作；两个筛选计数与 `/api/leads` 返回一致 |
| ③ | 批③ 顶栏收口 | `ca5e997` | 顶栏＝logo＋搜索；发送/回复箱/批量下拉/导入 CSV 各回所属阶段页；回复箱并入已回复页（**数据源改成 `/api/leads?group=replied`**，不再走 :614 那条 replies 查询）；删重复的客户画像入口 | ✅ 已回复页数据源确认走 `group=replied`；隐藏代理保留，无 dangling ref（全局搜过 `#btn-send`/`#btn-replies`） |
| ④ | 批③D 绿横幅 | `257311e` | 列表页不再横幅打断，建议统一进「待办事项」 | ✅ 列表页无横幅；建议出现在待办事项且带优先级 |
| ⑤ | 批③E 弹窗 | `6390fff` | 原生 `confirm()` 全换成 `uiConfirm()`，文案精准到"要对这 N 家做什么、影响什么" | ✅ 全站已无原生 confirm；逐个弹窗核过文案与实际动作一致 |
| ⑥ | 批④ 四页重构 | `7c605bd` `6a55711` `94c5c37` `e3a7c70` | 见下 | ✅ 四页逐条核过 |
| ⑦ | 免费源自动刷新 | `da822e4` | cron 每 6h 查一次、距上次 >7d 才刷 NMEA+rvwithtito，**零 Serper**，与付费搜索开关互相独立；找客户页加「每周自动刷新」勾选框 | ✅ 默认开；勾选框 roundtrip 通；**>7d 闸实测生效**（设成 3 天前 → cron 日志 `skipped: <7d`、未抓站）；该次 cron 全程零消耗 |

**批④ 四页明细**：
- **待办事项**（`7c605bd`）→ 分诊台：0 不显示；按紧急度排；「X 家能发」换成**真口径**（`/api/today` 新增 `sendable` ＝ approved ∧ 有邮箱 ∧ ≥60 ∧ 未 suppressed）；删掉新高分列表。实测：`approved_total=4` 但 `sendable=1` —— **"322 vs 41"那个谎正是这么来的，现在页面报的是真能发的数**。
- **数据看板**（`6a55711`）→ 体检报告：删「现在最该做」「状态分布」；**n<50 只显示计数不显示率**（`N_MIN=50`）；加收件箱切片；修「两个美国」。
- **找客户**（`94c5c37`）→ 加积压刹车条（`getBacklog` ＝ 未打分／缺邮箱／能发三个数）。
- **详情页**（`e3a7c70`）→ 动作跟所在格联动；缺邮箱时主按钮变「补邮箱」；**走 A1 同一条护栏**（`approveGateReason`）；补打开/点击状态；已发过的信折叠。

### 二、A 段后端护栏（总工已验过，此处只记）

`approveGateReason(email, score)` ＝「待发送」准入的**单一真源**，`bulk-status` 和 `:id/status` 共用；`no_reply` 已全局搜过无依赖后移除；`ids?: number[]` 加到 analyze/find-batch/send/followup 四个端点——**都是拼进原来那条带守卫的 WHERE**，≥60 下限、日额上限、原子 claim（approved→queued）、`deliverEmail` 幂等、`isEmailSuppressed` 一条没绕。

### 三、我自己发现并修掉的问题（不是这次需求，顺手修了）

1. **「两个美国」的写入口**（`src/service.ts`）：AI 回填国家时 `.toLowerCase()` 才落库（注释还写着"归一小写"），而发现入库写大写、列表筛选用 `UPPER(l.country)=?` 所以筛选没露馅，**只有看板 `GROUP BY country` 露了**。已在**所有写入口**改成落库大写（service 回填、landing `countryDb`、CSV `csvCountry`），并把看板 `GROUP BY UPPER(country)` 作为兜底。实测：库里 US(4)+us(1) 时看板已合成单个 US:5。**存量归一仍由总工在部署时跑，我没动生产库。**
2. **`export const APPROVE_MIN_SCORE` 会让 Worker 起不来**：入口模块顶层 export 非函数 → 运行时 `Incorrect type for map entry … not of type 'function or ExportedHandler'`，而 **`--dry-run` 照样绿**。已改成模块内 const ＋ 警告注释。（总工已把这条提成他的部署闸）
3. **`uiConfirm` 里字面量 `**` 会漏出来**：esc 之后没转换。修法是 `fmt()`＝**先 esc 再做受控转换**，顺序反了等于开 XSS 口子。

### 四、我不确定的地方（等总工定，我按规则没动手）

1. **⚠️ 抓站失败 ＝ 不合格，分数上不可区分（都 ≤30）** —— 这条最要紧，详见下方队列①条目末尾。**总工跑归位 SQL 后 199 条回 `new` 由 cron 重打分，谁那一刻恰好抓不到官网就被永久钉在 ≤30**，不重试、且长得跟"真不合格"一模一样。建议抓失败时不写 `match_score`（留 NULL 等下轮）或记 `fetch_failed` 标记。**因为它改动打分/写入行为，我只记录不动手。**
2. **「仅搜索(不分析)」我没删**（总工建议删，但说"如果发现别的正当用途先告诉我，别硬删"）。查下来：**199 条不是它造成的**——那批是 `approved` 却没分析记录；而"仅搜索"留下的是 `new`，cron 下一轮就会分析掉。加上 A1 护栏后（没分数进不了待发送）它已经无害。它的正当用途是**先低成本探一个词值不值得烧 AI 额度**。按「拿不准要不要删就不删」的规则 → 保留，理由写进了 `88acd4c`。
3. **`rvwithtito.com` 本轮抓不到**（队列①里 0 分那条也是它）。⑦ 的自动刷新我仍保留了这个源——不确定是它临时挂了、还是拦了我们的 UA。**下次真刷（>7d 后）如果它稳定 0 收获，这个源应该考虑摘掉。**

### 五、口径 / 顺序提醒（部署时）

- **H3-v2 的 UI 与打分必须先上线并生效，再跑归位 SQL** —— Joe 明确要求的顺序，否则 199 条会用旧闸重打。
- 存量迁移（199→new、82→analyzed、country 归一）**全部由总工在部署时跑，我一条没执行**。
- ⑦ 的 `directory_autorefresh_enabled` **默认开**（缺省即"1"），生产不需要额外写 settings 就会生效；如果不想它一上线就刷，部署时把 `directory_last_refresh` 写成当前时间即可推迟一周。

### 六、铁律执行情况（自查）

| 铁律 | 执行 |
|---|---|
| 别自己 deploy | ✅ 全程未跑 `wrangler deploy`（这条纯靠自觉，我守住了） |
| 别动生产数据 | ✅ 只用 `--local` 库；测试线索 9800-9807、测试 settings 都已清 |
| 动完 `.ts` 真起 8788 确认 boot | ✅ 每批都真起（200 + Ready + 零 Uncaught），没信 dry-run |
| 测发信三重保险 | ✅ 本轮**根本没碰发信端点的发信行为**（只加了 `ids?` 参数与前端），故无发信测试 |
| 不 `git add -f` 密钥 | ✅ `.dev.vars` / `.cf-token` 未进版本库 |
| 别碰 M3 / followup / discovery 开关 | ✅ 未改 |

**队列①～⑦ 全部完成，停在这里等总工回来看 git log。**

---

## 2026-07-15 · 队列①：H3-v2 打分闸 before/after 实测 ✅ 通过

**结论：误伤解除、老病没回来。H3-v2 可以留。**

### 改了什么（代码已在 baseline e9ae402 里）

按 Joe 拍板的 A 方案改 `src/openrouter.ts` 的 `scoreLead` prompt：

- **删掉**高分硬门槛里的「② 规模是 中小/区域型（不是全国性巨头）」
- **删掉**三条按体量压的一票压低（全国性电信/ISP巨头、连锁大卖场、航天/企业级卫星巨头）
- **换成**唯一判据：**官网能不能看出它在「卖」、或者在「装」，星链/卫星实体硬件**。看得出来就按契合度正常给分，**不看体量**——三个人的安装队和 speedcast 同等对待
- 一票压低改为按"会不会买配件"：纯内容/攻略/评测站、**只卖自家网络的运营商（竞品无需求）**、中国同行铺货低价卖家、看不出卖/装任何实体硬件的、非真实经营实体
- buyer_type 取证要求从"是不是巨头"换成"**在卖/装什么实体硬件**"

### 实测（本地 dev 库，真抓官网 + 真 OpenRouter；未碰生产库）

**有效规则判定**（官网抓取成功、模型真判了）：

| 公司 | before | after | after 的 buyer_type 理由 | 结论 |
|---|---|---|---|---|
| **Speedcast** | 30 | **85** | 【合格·授权 Starlink Reseller 和集成商】"官网明确显示为授权 Starlink Reseller 和集成商…需要实体配件进行安装和维护" | ✅ **误伤解除**（Joe 点名的那家） |
| **Telstra** | 30 | **30** | 【不合格·卖自家网络的电信/宽带/ISP 运营商（竞品无需求）】"销售自家网络服务，星链是其竞争对手，不会采购星链配件" | ✅ **仍正确压低，且理由已从"巨头"换成"竞品"** |
| **Harvey Norman** (.ie) | 30 | **30** | 【不合格·官网看不出在卖或装任何实体硬件】"官网信息含糊，没有显示销售或安装星链或相关实体硬件的证据" | ✅ 理由已不是"连锁零售巨头"；.ie 站确实无星链证据 |
| **Johnson Hicks Marine** | 75 | **75** | 【合格·经销商/分销商】"船舶电子设备经销商…且有安装服务" | ✅ **中小商未误伤** |
| **Starlink Insider** | （新增） | **20** | 【不合格·纯内容/攻略/评测/新闻站】"仅为提供 Starlink 相关新闻、教程和资源的内容站，无任何实体产品销售或安装服务" | ✅ **老病没回来**（关键试金石） |

**两头都验到了**：巨头（Speedcast 30→85）抬起来了；纯内容站（Starlink Insider 20）仍被压死。

### 过程中发现并修掉的一个自己的 bug

v2 初版我把「卖自家网络＝竞品」写成了**独立条件**，它盖过了总工定的唯一判据 →
**Speedcast 首测被判"竞品"压到 30**（正是要救的那家被自己的新规则又埋了一次）。
修法：让**硬件证据压过竞品规则**——竞品条加了硬约束"⚠️ 本条只在『官网看不出它在卖或安装任何卫星/星链实体硬件』时才成立；如果它同时在装/卖卫星硬件（海事/航空卫星集成商、装天线终端、VSAT/Starlink 终端安装或转售），那它就是目标客户，本条不适用"。
重测：**Speedcast 30 → 85**。这条修正很关键，否则 A 方案等于没改。

### ⚠️ 发现的问题（不是我改坏的，但会咬人，总工回来看一下）

**抓站失败与"不合格"在分数上不可区分——都是 ≤30。**

本轮有 3 家的低分**不是规则判的，是官网抓不到**：

| 公司 | 分 | 实际原因 |
|---|---|---|
| Telespazio | 30 | 【官网内容**无法抓取**】 |
| Seacoast Marine | 0 / 30 | 【官网**无法访问**】—— ⚠️ 它在 H3-v1 那轮**抓得到**、拿了 75（"专注船舶通信和导航设备的经销商和安装商"），本轮连续两次抓失败 |
| RV With Tito | 0 | 【官网无法访问】 |

Seacoast 从"抓得到 75"变成"抓不到 0"，中间只隔一小时 → 疑似**被目标站限流/拦 UA**（我这轮对同一批站点反复抓过多次）。

**为什么这条要紧**：总工跑归位 SQL 后，199 条回 `new`、cron 会按 H3 重新打分。**一条线索如果那一刻恰好抓不到官网，就会被永久钉在 ≤30**——不会自动重试、也和"真不合格"长得一模一样，从此埋在待分析/低分堆里再没人管。
**建议（我没动，等你定）**：把"抓站失败"与"判定不合格"分开——例如抓失败时不写 `match_score`（保持 NULL、留在待分析等下轮重试），或在 `lead_analysis` 记一个 `fetch_failed` 标记，别让网络抖动变成永久判决。这条会改动打分/写入行为，按规则我只记录不动手。

### 其它

- 铁律照守：动完 `.ts` **真起 8788 确认 boot**（200 + Ready + 零 Uncaught）才继续，没只信 dry-run
- 测试线索 9800-9807 已全删；生产库未碰；`.dev.vars` 未动

---
